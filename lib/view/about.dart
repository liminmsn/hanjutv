import 'package:flutter/material.dart';

class ViewAbout extends StatelessWidget {
  const ViewAbout({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Align(child: Text("hello")));
  }
}
